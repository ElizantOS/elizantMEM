# elizantMEM

Research repository for the `Memory as Policy` direction.

This repo contains:

- the protected research intent in `goal.md`
- the research loop operating policy in `program.md`
- sentinel-owned control files under `control/`
- the current paper/design/experiment workspace under `workspace/`
- executable experiment scaffolding under `implementation/`
- the current evaluator in `evaluation/evaluate.ts`

Current stage:

- literature grounding completed
- design and experiment contract established
- sentinel has already advanced the lane to `experiment_bootstrap`
- next work should focus on trace, ledger, replay, baseline, and metric execution surfaces

Key commands:

```bash
bun run evaluate
```
