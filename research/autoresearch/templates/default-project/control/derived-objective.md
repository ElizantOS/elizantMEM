# Sentinel-Derived Objective

This file is owned by sentinel.

The protected source intent stays in `goal.md`. Sentinel can refine this file into a more measurable optimization target once the research environment is stable.

Default policy:

1. ground the project in literature and benchmark evidence first
2. derive design and experiment contracts from that evidence
3. only then optimize paper and results artifacts for coherence

`autoresearch init` rewrites this file during sentinel bootstrap so the project starts with a concrete derived target.
