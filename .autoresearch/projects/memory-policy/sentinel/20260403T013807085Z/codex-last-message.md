Updated the sentinel maintenance path in [scripts/autoresearch.ts](/Users/echo/project/Elizant/claude-code-sourcemap-main/gpt-code/scripts/autoresearch.ts). The main fix is diagnostic: sentinel now distinguishes generic stagnation from a `dry-run-noop-loop`, so the current `memory-policy` state is reported as “three dry-run no-op iterations with zero file changes” instead of only “no accepted run.” I also expanded version tracking to include the full maintenance surface (`README` and templates), added promoted-run / streak summary fields, and made the dashboard show evaluator metrics and a more actionable maintenance queue.

I regenerated the runtime artifacts at [dashboard.html](/Users/echo/project/Elizant/claude-code-sourcemap-main/gpt-code/.autoresearch/projects/memory-policy/dashboard.html), [health.json](/Users/echo/project/Elizant/claude-code-sourcemap-main/gpt-code/.autoresearch/projects/memory-policy/health.json), [health.md](/Users/echo/project/Elizant/claude-code-sourcemap-main/gpt-code/.autoresearch/projects/memory-policy/health.md), and [maintenance-queue.json](/Users/echo/project/Elizant/claude-code-sourcemap-main/gpt-code/.autoresearch/projects/memory-policy/maintenance-queue.json). No protected project content was edited.

Simplifications made: I kept the change inside the controller only, reused the existing stagnation threshold for the new no-op detection, and avoided adding new config fields or dependencies.

Verification: `bun run autoresearch sentinel memory-policy` passed and the new health output now reports `warning:stagnation, warning:dry-run-noop-loop`. Full repo typecheck via `bunx tsc --noEmit --project tsconfig.json` still fails, but only because the repository already has many unrelated TypeScript errors outside the autoresearch controller.